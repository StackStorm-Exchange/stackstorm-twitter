---
  name: "direct_message"
  runner_type: "python-script"
  description: "Direct message a user."
  enabled: true
  entry_point: "direct_message.py"
  parameters:
    screen_name:
      type: "string"
      description: "screen name of the user to direct message."
      required: true
    message:
      type: "string"
      description: "The message to send."
      required: true
    account:
      type: "string"
      description: "Account to send direct message from. Overrides default account in config."
      required: false