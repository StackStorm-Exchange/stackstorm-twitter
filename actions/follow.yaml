---
  name: "follow"
  runner_type: "python-script"
  description: "Follow a user."
  enabled: true
  entry_point: "follow.py"
  parameters:
    screen_name:
      type: "string"
      description: "Screen name of the user to follow."
      required: true
    account:
      type: "string"
      description: "Account to follow from. Overrides default account in config."
      required: false
